<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Componentes_ProyectoGimnasioMMA.Componentes.GestionPersonas.SelectorAlumno">
    <Grid Padding="20" RowDefinitions="Auto, *">

        <!-- Título y Picker -->
        <Label Text="Seleccione un Alumno"
               HorizontalOptions="Center" 
               Grid.Row="0"/>

        <!-- ScrollView -->
        <ScrollView x:Name="ScrollViewUsuarios" 
                    VerticalOptions="FillAndExpand" 
                    Grid.Row="1">
            <VerticalStackLayout x:Name="VerticalStackLayoutEscuelas" 
                                 Spacing="15" 
                                 VerticalOptions="Start"/>
        </ScrollView>

    </Grid>
</ContentView>
